<UserControl x:Class="Linqua.WordListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lp="clr-namespace:Linqua;assembly=Linqua.Portable"
             xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
             xmlns:l="clr-namespace:Linqua"
             mc:Ignorable="d"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480"
             d:DesignWidth="480"
			 d:DataContext="{d:DesignInstance lp:WordListViewModel, IsDesignTimeCreatable=True}">

    <Grid x:Name="LayoutRoot"
          Background="{StaticResource PhoneChromeBrush}">
		
		<ListBox ItemsSource="{Binding Words}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<l:WordListItemView DataContext="{Binding}"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>