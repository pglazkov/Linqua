<Page x:Class="Linqua.MainPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:Linqua"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:i="using:Microsoft.Xaml.Interactivity"
	  xmlns:framework="using:Linqua.Framework"
	  xmlns:framework1="using:Framework"
	  mc:Ignorable="d"
	  Background="{StaticResource ApplicationThemeBrush}"
	  d:DataContext="{d:DesignInstance local:MainViewModel, IsDesignTimeCreatable=True}"
	  RequestedTheme="Light">

	<!--<i:Interaction.Behaviors>
		<framework:StatusBarBehavior BackgroundColor="{StaticResource ApplicationThemeColor}"
									 ForegroundColor="White"/>
	</i:Interaction.Behaviors>-->
	
	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot"
		  Background="White">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

		<!--TitlePanel contains the name of the application and page title-->
		<Border Grid.Row="0"
				Background="{StaticResource ApplicationThemeBrush}">
			<StackPanel x:Name="TitlePanel"
						Margin="12,17,0,17">
				<TextBlock x:Uid="ApplicationHeader"
						   Text="LINQUA"
						   Style="{ThemeResource TitleTextBlockStyle}"
						   Foreground="White"
						   Margin="12,0" />
				<TextBlock Text="my words"
						   Margin="9,-7,0,0"
						   Style="{ThemeResource HeaderTextBlockStyle}"
						   Foreground="White"/>
			</StackPanel>
		</Border>

		<!--ContentPanel - place additional content here-->
		<Grid x:Name="ContentPanel"
			  Grid.Row="1"
			  Margin="24,12,24,0">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			
			<TextBlock Grid.Row="0" 
					   x:Uid="LoadingEntriesTextBlock" 
					   Style="{ThemeResource ListViewEmptyStaticTextBlockStyle}" 
					   Visibility="{Binding IsLoadingEntries, Converter={StaticResource BooleanToVisibilityConverter}}"
					   Text="Please Wait..."
					   VerticalAlignment="Center"
					   HorizontalAlignment="Center"/>

			<ContentPresenter Grid.Row="0"
							  x:Name="EntryListView"
							  framework1:ViewLocator.Model="{Binding EntryListViewModel}"/>
			
			<!--<local:EntryListView Grid.Row="0" 
								 x:Name="EntryListView" 
								 DataContext="{Binding EntryListViewModel}" />-->

			<local:EntryCreationView Grid.Row="1"
									 DataContext="{Binding EntryCreationViewModel}"
									 Margin="0,24,0,12"
									 IsEnabled="{Binding IsLoadingEntries, Converter={StaticResource InvertedBooleanConverter}}"/>
		</Grid>

		<!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
		<!--<Image Source="/Assets/AlignmentGrid.png"
			   VerticalAlignment="Top"
			   Height="800"
			   Width="480"
			   Margin="0,-32,0,0"
			   Grid.Row="0"
			   Grid.RowSpan="2"
			   IsHitTestVisible="False" />-->
	</Grid>

	<!--<Page.BottomAppBar>
		<CommandBar>
			<AppBarButton x:Name="NewWordButton" 
						  Content="new word"
						  Icon="Add"
						  Command="{Binding AddWordCommand}"/>
		</CommandBar>
	</Page.BottomAppBar>-->
</Page>