<UserControl x:Class="Linqua.WordListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:linqua="using:Linqua"
             mc:Ignorable="d"
             d:DesignHeight="480"
             d:DesignWidth="480"
			 d:DataContext="{d:DesignInstance linqua:WordListViewModel, IsDesignTimeCreatable=True}">

    <Grid x:Name="LayoutRoot"
          Background="{ThemeResource PhoneChromeBrush}">
		
		<ListBox ItemsSource="{Binding Words}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<linqua:WordListItemView DataContext="{Binding}"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>