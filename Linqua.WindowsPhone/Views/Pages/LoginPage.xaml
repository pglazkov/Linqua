<Page x:Class="Linqua.LoginPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:Linqua"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  mc:Ignorable="d"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	  d:DataContext="{d:DesignInstance local:LoginViewModel, d:IsDesignTimeCreatable=True}">

	<Grid VerticalAlignment="Center">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<ProgressRing Grid.Row="0"
					  IsActive="True"
					  Background="Transparent"
					  Foreground="{StaticResource ApplicationThemeBrush}"
					  Margin="12,0,12,0" />

		<TextBlock Grid.Row="1"
				   Text="Logging in..."
				   Margin="0,12,0,0"
				   Style="{ThemeResource GroupHeaderTextBlockStyle}"
				   HorizontalAlignment="Center"
				   Foreground="{StaticResource ApplicationThemeBrush}" />


	</Grid>
</Page>
