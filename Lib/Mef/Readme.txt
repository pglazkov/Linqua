This is a portable library targeting .NET 4.5 + Windows Store + Windows Phone 8.

The library is built from sources in order to include the support for Windows Phone 8 (which is not included by default).